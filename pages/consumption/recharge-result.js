// offer purchase result page defintion

import * as React from "react";

import { PubLayout, PurchaseResult } from "../../src/components";

const StartPage = ({}) => {
  return (
    <>
      <PubLayout>
        <PurchaseResult />
      </PubLayout>
    </>
  );
};

export default StartPage;
