// offer Price list page defintion

import * as React from "react";

import { PubLayout, PriceList } from "../../src/components";

const StartPage = ({}) => {
  return (
    <>
      <PubLayout>
        <PriceList />
      </PubLayout>
    </>
  );
};

export default StartPage;
