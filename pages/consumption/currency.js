// payment canal page defintion

import * as React from "react";

import { PubLayout, PaymentCurrency } from "../../src/components";

const StartPage = ({}) => {
  return (
    <>
      <PubLayout>
        <PaymentCurrency />
      </PubLayout>
    </>
  );
};

export default StartPage;
