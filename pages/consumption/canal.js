// payment canal page defintion

import * as React from "react";

import { PubLayout, PaymentCanal } from "../../src/components";

const StartPage = ({}) => {
  return (
    <>
      <PubLayout>
        <PaymentCanal />
      </PubLayout>
    </>
  );
};

export default StartPage;
